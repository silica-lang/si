# Edit: add a second LED and speed up

Given the `blink` program in `before.si`, edit it so that:

1. the period is 250 ms (was 500 ms), and
2. a second boolean `cell` named `lit2` (init `false`) toggles at the same time
   as `lit`.

Keep it idiomatic — no escape hatches.
