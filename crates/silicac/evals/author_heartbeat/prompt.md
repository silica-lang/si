# Author: heartbeat

Write a Silica program named `heartbeat` with a single boolean `cell` called
`lit`, initialised to `false`, that toggles every 500 ms.

Idiomatic constraints: use the reactive core (`every`) and the boolean `not`
operator — no casts or other escape hatches are needed.
