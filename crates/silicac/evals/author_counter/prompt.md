# Author: counter

Write a Silica program named `counter` with a `u32` `cell` called `ticks`,
initialised to `0`, that increments by 1 every 100 ms.

Idiomatic constraints: plain `u32` arithmetic — no casts, wrapping, or
saturating operators are needed for a simple monotonic counter.
