// Reference solution — debug_narrowing.  The narrowing is made explicit with a
// single `as u8` cast (the one visible escape hatch for a truncating conversion).
program narrow {
  cell total : u32 = 0
  cell low : u8 = 0

  every 100ms {
    total = total + 100
    low = total as u8
  }
}
